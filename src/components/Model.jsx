import React from 'react';
import DeviceControl from './DeviceControl';

const Animated3DModel = ({ modelPath }) => {
  return (
    <DeviceControl modelPath={modelPath} />
  );
};

export default Animated3DModel;
